{
  "name": "cloudtriangles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler pages dev -- vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "lint": "eslint .",
    "deploy": "npm run build && wrangler pages deploy dist --project-name cloudtriangles --commit-dirty=true"
  },
  "dependencies": {
    "axios": "^1.13",
    "vue": "^3.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6",
    "eslint": "^9",
    "eslint-plugin-vue": "^10",
    "vite": "^7",
    "wrangler": "^4"
  }
}
